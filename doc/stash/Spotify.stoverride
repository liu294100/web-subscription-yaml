name: Spotify Override
desc: 独立的 Spotify 分流规则，包含专用的策略组
rule-providers:
  Spotify:
    type: http
    behavior: classical
    url: https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Clash/Spotify/Spotify.yaml
    interval: 86400

proxy-groups:
- name: 🎵 SpotifyRule
  type: select
  proxies: []
  include-all-proxies: true
  # 默认筛选包含“Spotify”或“US”等关键词的节点，如果想选所有节点，可以用 .*
  # 为了防止没有节点，这里使用 .* 匹配所有节点
  filter: .*

rules:
- RULE-SET,Spotify,🎵 SpotifyRule
